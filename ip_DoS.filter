if (ip.proto == UDP) {
	if (search(DECODED.data, "BJNP")) {
		log(DATA.data, "/tmp/DoSUDP.log");
		drop();
		msg("drop UDP");
	}
}

if (ip.proto == TCP) {
	if (search(DECODED.data, "BJNP")) {
		drop();
		log(DATA.data, "/tmp/DoSTCP.log");
		msg("drop TCP");
	}
}
